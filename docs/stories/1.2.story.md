# Story 1.2: Database Setup

## Status

Done

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["docs/architecture/database-schema.md", "docs/architecture/coding-standards.md"]
```

## Story

**As a** desenvolvedor,
**I want** Drizzle ORM configurado com better-sqlite3 e o schema de livros definido com migration aplicada,
**so that** as stories de Books tenham uma camada de dados tipada, testável e com histórico auditável.

## Complexity

**Estimativa:** S (Small) — Schema único, migration linear, sem UI. Depende da Story 1.1 completa.

## Scope

**IN:**
- Instalar Drizzle ORM, drizzle-kit, better-sqlite3, ulidx
- Criar `drizzle.config.ts`, `src/lib/db/schema.ts`, `src/lib/db/index.ts`
- Gerar e aplicar migration da tabela `books`
- Exportar tipos `Book` e `NewBook` de `src/types/index.ts`
- Testes unitários TDD para validação de tipos TypeScript

**OUT:**
- Queries CQRS (Stories 3.x)
- CRUD operations ou Server Actions (Stories 3.x)
- Múltiplas tabelas além de `books`
- Autenticação ou autorização
- Interface de administração do banco (Drizzle Studio apenas como dev-tool)

## Risks

| Risco | Probabilidade | Impacto | Mitigação |
|-------|--------------|---------|-----------|
| WAL mode com problemas em filesystem Windows (OneDrive/network drive) | Baixa | Alto | Garantir que `data/` esteja em disco local, não sincronizado |
| Versão do `better-sqlite3` incompatível com Node.js do projeto | Baixa | Médio | Verificar engines no package.json após instalação |
| Depende de Story 1.1 (diretórios `src/lib/db/`, `src/types/`) | Alta | Bloqueante | Executar somente após Story 1.1 Done |

## Acceptance Criteria

1. `better-sqlite3`, `drizzle-orm` e `drizzle-kit` instalados
2. `drizzle.config.ts` aponta para `data/homeos.db` e `src/lib/db/migrations/`
3. `src/lib/db/schema.ts` define tabela `books` com: `id` (TEXT, ULID, PK), `title` (TEXT NOT NULL), `author` (TEXT NOT NULL), `release_year` (INTEGER nullable), `reading_year` (INTEGER nullable), `status` (TEXT enum: unread/reading/read, default: unread), `cover_path` (TEXT nullable), `synopsis` (TEXT nullable), `notes` (TEXT nullable), `created_at` (TEXT NOT NULL), `updated_at` (TEXT NOT NULL)
4. `src/lib/db/index.ts` exporta singleton do DB seguro para hot reload do Next.js com WAL mode habilitado
5. `npm run db:generate` produz migration em `src/lib/db/migrations/`
6. `npm run db:migrate` aplica a migration e cria `data/homeos.db`
7. `data/homeos.db` e `data/covers/` adicionados ao `.gitignore`; arquivos de migration **não** estão no `.gitignore`
8. Tipos `Book` e `NewBook` exportados de `src/types/index.ts` (inferidos do schema Drizzle)
9. Testes unitários (TDD) para o schema: validar que os tipos TypeScript possuem os campos esperados

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [x] Task 1: Escrever testes unitários do schema (TDD — FIRST) (AC: 9)
  - [x] Criar `tests/unit/db/schema.test.ts` ANTES de criar o schema
  - [x] Teste valida que o tipo `Book` tem campos: `id`, `title`, `author`, `release_year`, `reading_year`, `status`, `cover_path`, `synopsis`, `notes`, `created_at`, `updated_at`
  - [x] Teste valida que `status` aceita apenas `"unread" | "reading" | "read"`
  - [x] Executar `npm run test` — deve falhar (Red)

- [x] Task 2: Instalar dependências (AC: 1)
  - [x] `npm install drizzle-orm better-sqlite3`
  - [x] `npm install -D drizzle-kit @types/better-sqlite3`
  - [x] `npm install ulidx` (geração de IDs ULID para commands futuros)

- [x] Task 3: Criar `drizzle.config.ts` (AC: 2)
  - [x] Criar arquivo na raiz do projeto apontando para `data/homeos.db` e `src/lib/db/migrations/`
  - [x] Dialect: `sqlite`

- [x] Task 4: Criar `src/lib/db/schema.ts` (AC: 3)
  - [x] Definir tabela `books` com todos os campos listados no AC 3
  - [x] Usar `text("status", { enum: ["unread", "reading", "read"] })` para o campo status
  - [x] Adicionar índices em `status` e `author` via `index()` do Drizzle
  - [x] Executar testes — deve passar (Green)

- [x] Task 5: Criar `src/lib/db/index.ts` — singleton seguro (AC: 4)
  - [x] Implementar padrão `globalThis` para evitar múltiplas conexões em hot reload
  - [x] Habilitar WAL mode: `sqlite.pragma("journal_mode = WAL")`
  - [x] Exportar `db` como instância do Drizzle com schema

- [x] Task 6: Adicionar scripts ao `package.json` (AC: 5, 6)
  - [x] `"db:generate": "drizzle-kit generate"`
  - [x] `"db:migrate": "drizzle-kit migrate"`
  - [x] `"db:studio": "drizzle-kit studio"`

- [x] Task 7: Gerar e aplicar migration (AC: 5, 6)
  - [x] Executar `npm run db:generate` — deve criar arquivo em `src/lib/db/migrations/`
  - [x] Executar `npm run db:migrate` — deve criar `data/homeos.db`
  - [x] Verificar que o arquivo de migration foi gerado com SQL correto

- [x] Task 8: Atualizar `.gitignore` (AC: 7)
  - [x] Confirmar entradas: `data/homeos.db`, `data/homeos.db-wal`, `data/homeos.db-shm`
  - [x] Confirmar que `src/lib/db/migrations/` NÃO está no `.gitignore`
  - [x] Confirmar que `data/covers/.gitkeep` está no git (covers/* ignorado, .gitkeep não)

- [x] Task 9: Criar `src/types/index.ts` com tipos inferidos (AC: 8)
  - [x] Exportar `Book` via `InferSelectModel<typeof books>`
  - [x] Exportar `NewBook` via `InferInsertModel<typeof books>`
  - [x] Verificar que os testes do Task 1 passam com os tipos exportados

## Dev Notes

### Dependências e Versões

```bash
npm install drizzle-orm better-sqlite3 ulidx
npm install -D drizzle-kit @types/better-sqlite3
```

[Source: docs/architecture/tech-stack.md#banco-de-dados]

### Schema Completo — `src/lib/db/schema.ts`

```typescript
import { sqliteTable, text, integer, index } from "drizzle-orm/sqlite-core";

export const books = sqliteTable(
  "books",
  {
    id: text("id").primaryKey(),
    title: text("title").notNull(),
    author: text("author").notNull(),
    release_year: integer("release_year"),
    reading_year: integer("reading_year"),
    status: text("status", { enum: ["unread", "reading", "read"] })
      .notNull()
      .default("unread"),
    cover_path: text("cover_path"),
    synopsis: text("synopsis"),
    notes: text("notes"),
    created_at: text("created_at").notNull(),
    updated_at: text("updated_at").notNull(),
  },
  (table) => ({
    statusIdx: index("books_status_idx").on(table.status),
    authorIdx: index("books_author_idx").on(table.author),
  })
);
```

[Source: docs/architecture/database-schema.md#schema-drizzle]

### Singleton do DB — `src/lib/db/index.ts`

**CRÍTICO:** O padrão `globalThis` é obrigatório para evitar múltiplas conexões SQLite durante hot reload do Next.js em desenvolvimento.

```typescript
import Database from "better-sqlite3";
import { drizzle } from "drizzle-orm/better-sqlite3";
import { join } from "node:path";
import * as schema from "./schema";

const globalForDb = globalThis as unknown as {
  db: ReturnType<typeof createDb> | undefined;
};

function createDb() {
  const dbPath = join(process.cwd(), "data", "homeos.db");
  const sqlite = new Database(dbPath);
  sqlite.pragma("journal_mode = WAL");
  return drizzle(sqlite, { schema });
}

export const db = globalForDb.db ?? createDb();

if (process.env.NODE_ENV !== "production") {
  globalForDb.db = db;
}
```

[Source: docs/architecture/coding-standards.md#6-singleton-do-db---padrão-seguro-para-hot-reload]

### `drizzle.config.ts`

```typescript
import type { Config } from "drizzle-kit";

export default {
  schema: "./src/lib/db/schema.ts",
  out: "./src/lib/db/migrations",
  dialect: "sqlite",
  dbCredentials: {
    url: "./data/homeos.db",
  },
} satisfies Config;
```

[Source: docs/architecture/database-schema.md#configuração-do-drizzle-kit]

### `src/types/index.ts`

```typescript
import type { InferSelectModel, InferInsertModel } from "drizzle-orm";
import type { books } from "@/lib/db/schema";

export type Book = InferSelectModel<typeof books>;
export type NewBook = InferInsertModel<typeof books>;
```

[Source: docs/architecture/database-schema.md#tipos-inferidos]

### Política de Migrations

- Arquivo gerado em `src/lib/db/migrations/` DEVE ser commitado — não está no `.gitignore`
- Nunca escrever SQL manualmente — sempre usar `drizzle-kit generate`
- Cada mudança de schema = nova migration (nunca modificar migration existente)

[Source: docs/architecture/database-schema.md#política-de-migrations]

### .gitignore — Entradas desta Story

```gitignore
data/homeos.db
data/homeos.db-wal
data/homeos.db-shm
data/covers/*
!data/covers/.gitkeep
```

Já devem existir da Story 1.1. Confirmar apenas.

### Testing

- **Localização dos testes**: `tests/unit/db/schema.test.ts`
- **Framework**: Vitest (sem DOM, environment padrão `node`)
- **Padrão TDD**: escrever testes ANTES do schema (Red → Green)
- **O que testar**: validar que os tipos TypeScript inferidos têm os campos corretos usando `satisfies` ou asserções de tipo

Exemplo de teste de tipo com Vitest:

```typescript
import { describe, it, expectTypeOf } from "vitest";
import type { Book } from "@/types";

describe("Book type", () => {
  it("possui todos os campos obrigatórios", () => {
    expectTypeOf<Book>().toHaveProperty("id");
    expectTypeOf<Book>().toHaveProperty("title");
    expectTypeOf<Book>().toHaveProperty("author");
    expectTypeOf<Book>().toHaveProperty("status");
    expectTypeOf<Book>().toHaveProperty("created_at");
    expectTypeOf<Book>().toHaveProperty("updated_at");
  });

  it("status é union type correto", () => {
    expectTypeOf<Book["status"]>().toEqualTypeOf<"unread" | "reading" | "read">();
  });

  it("campos opcionais são nullable", () => {
    expectTypeOf<Book["release_year"]>().toEqualTypeOf<number | null>();
    expectTypeOf<Book["cover_path"]>().toEqualTypeOf<string | null>();
  });
});
```

[Source: docs/architecture/coding-standards.md#4-padrão-de-testes-tdd]

### Dependência de Story Anterior

Esta story depende da Story 1.1 estar completa (diretório `src/lib/db/` e `src/types/` devem existir, `.gitignore` base configurado).

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-03-25 | 1.0 | Story criada | River (@sm) |
| 2026-03-27 | 1.1 | Validação PO — GO (8/10). Should-fix: estimativa de complexidade, riscos, escopo OUT | Pax (@po) |
| 2026-03-27 | 1.2 | Adicionadas seções Complexity (S), Scope (IN/OUT) e Risks — correção should-fix do @po | River (@sm) |
| 2026-03-27 | 1.3 | Implementação completa — todas as 9 tasks concluídas, testes passando, build OK | Dex (@dev) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

- `tests/unit/db/schema.test.ts`: Vitest `expectTypeOf` usa type erasure — testes TDD passam em runtime sem a implementação. Red phase confirmado via `tsc --noEmit` (falha sem `src/types/index.ts`). Green confirmado após criação de todos os arquivos.
- `drizzle-kit generate` gerou migration como `0000_cooing_carlie_cooper.sql` (nome auto-gerado pelo Drizzle).

### Completion Notes List

- `drizzle-orm@0.45.2`, `better-sqlite3@12.8.0`, `drizzle-kit@0.31.10`, `ulidx@2.4.1` instalados
- `data/homeos.db` criado com WAL mode após `npm run db:migrate`
- Migration gerada em `src/lib/db/migrations/0000_cooing_carlie_cooper.sql`
- `.gitignore` já correto da Story 1.1 — nenhuma alteração necessária
- Task 9 (tipos) executada antes da Task 7 (migration) para satisfazer os testes de tipo mais cedo

### File List

**Criados:**
- `homeos/drizzle.config.ts` — configuração do Drizzle Kit
- `homeos/src/lib/db/schema.ts` — schema da tabela `books` com índices
- `homeos/src/lib/db/index.ts` — singleton do DB com padrão globalThis e WAL mode
- `homeos/src/lib/db/migrations/0000_cooing_carlie_cooper.sql` — migration gerada
- `homeos/src/lib/db/migrations/meta/` — metadados do Drizzle Kit
- `homeos/src/types/index.ts` — tipos `Book` e `NewBook` inferidos do schema
- `homeos/tests/unit/db/schema.test.ts` — testes TDD de validação de tipos
- `homeos/data/homeos.db` — banco SQLite (gitignored)

**Modificados:**
- `homeos/package.json` — adicionados scripts `db:generate`, `db:migrate`, `db:studio`

## QA Results

### Review Date: 2026-03-27

### Reviewed By: Quinn (Test Architect)

**Acceptance Criteria — Traceability:**

| AC | Descrição | Status |
|----|-----------|--------|
| 1 | Dependências instaladas (`drizzle-orm@0.45.2`, `better-sqlite3@12.8.0`, `drizzle-kit@0.31.10`, `ulidx@2.4.1`) | ✅ PASS |
| 2 | `drizzle.config.ts` com dialect `sqlite`, aponta para `data/homeos.db` e `src/lib/db/migrations/` | ✅ PASS |
| 3 | Schema `books` com 11 colunas corretas + 2 índices (`status`, `author`) | ✅ PASS |
| 4 | Singleton com padrão `globalThis` + `pragma("journal_mode = WAL")` | ✅ PASS |
| 5 | `npm run db:generate` → migration `0000_cooing_carlie_cooper.sql` gerada | ✅ PASS |
| 6 | `npm run db:migrate` → `data/homeos.db` criado | ✅ PASS |
| 7 | `data/homeos.db` no `.gitignore`; migrations fora do `.gitignore` | ✅ PASS |
| 8 | `Book` = `InferSelectModel`, `NewBook` = `InferInsertModel` em `src/types/index.ts` | ✅ PASS |
| 9 | 3 testes de tipo em `tests/unit/db/schema.test.ts`, todos passando | ✅ PASS |

**Validações executadas:** `npm run test` (5/5 ✅) · `tsc --noEmit` (sem erros ✅) · `npm run lint` (sem erros ✅)

**SQL da migration verificado:** `CREATE TABLE books` com todas as colunas, constraints e índices corretos.

**JSDoc:** `src/lib/db/index.ts` tem JSDoc com `@design` explicando o padrão globalThis e a referência à documentação do Prisma. Padrão de documentação seguido conforme coding-standards.

### Gate Status

Gate: PASS → docs/qa/gates/1.2-database-setup.yml
