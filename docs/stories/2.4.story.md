# Story 2.4: Showcase: Overlays e Feedback

## Status

Ready

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["docs/architecture/tech-stack.md", "docs/architecture/source-tree.md"]
```

## Story

**As a** usuário,
**I want** ver e disparar os componentes de overlay e feedback do Design System,
**so that** eu possa verificar o comportamento de modais, sheets e notificações.

## Complexity

**Estimativa:** M (Medium) — componentes com comportamento de abertura/fechamento, integração com Toaster provider no root layout.

## Scope

**IN:**
- Seção "Overlays": `Dialog`, `Sheet`, `Tooltip`, `Popover`, `DropdownMenu`
- Seção "Feedback": `Toast` (com botão trigger), `Alert` (variantes), `Skeleton`, `Progress`
- Cada overlay abre e fecha via interação com trigger
- Toast disparado por botão e desaparece após timeout
- Verificar se `Toaster` está no root layout; adicionar se ausente
- 1 teste de componente (TDD): triggers de Dialog e Sheet presentes e clicáveis

**OUT:**
- Formulários dentro de Dialog
- Implementação de Toast como sistema de notificação global da aplicação
- Customização de Progress com dados reais

## Risks

| Risco | Probabilidade | Impacto | Mitigação |
|-------|--------------|---------|-----------|
| Toaster não está no root layout | Média | Alto | Verificar `src/components/providers.tsx` e `src/app/layout.tsx`; adicionar se necessário |
| Componentes Radix usam portals | Baixa | Baixo | Testing Library + jsdom suporta portals nativamente |
| shadcn/ui usa Sonner para toast (não o `useToast` legado) | Baixa | Baixo | Verificar qual está instalado; shadcn/ui atual usa Sonner por padrão |

## Acceptance Criteria

1. Seção "Overlays" exibe: `Dialog`, `Sheet`, `Tooltip`, `Popover`, `DropdownMenu`
2. Seção "Feedback" exibe: `Toast` (com botão para disparar), `Alert` (todas variantes), `Skeleton`, `Progress`
3. Cada overlay abre e fecha corretamente ao interagir com o trigger
4. Toast pode ser disparado via botão e desaparece após timeout
5. Teste de componente (TDD): validar que triggers de Dialog e Sheet estão presentes e são clicáveis

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI não está habilitado em `core-config.yaml`.
> Quality validation usará processo de revisão manual.

## Tasks / Subtasks

- [ ] Task 1: Verificar e instalar componentes shadcn/ui necessários
  - [ ] Listar `src/components/ui/` — checar: dialog, sheet, tooltip, popover, dropdown-menu, sonner, alert, skeleton, progress
  - [ ] Instalar os ausentes: `npx shadcn@latest add dialog sheet tooltip popover dropdown-menu sonner alert skeleton progress` (somente os que faltarem)

- [ ] Task 2: Verificar/adicionar Toaster ao root layout (AC: 4)
  - [ ] Ler `src/components/providers.tsx` e `src/app/layout.tsx`
  - [ ] Se `<Toaster />` (Sonner) não estiver presente, adicionar em `providers.tsx`

- [ ] Task 3: Escrever teste de componente (TDD — ANTES da implementação) (AC: 5)
  - [ ] Criar `tests/component/overlays-section.test.tsx`
  - [ ] Testar presença de trigger button para Dialog (ex: `getByRole('button', { name: /abrir dialog/i })`)
  - [ ] Testar presença de trigger button para Sheet
  - [ ] Executar `npm run test` — deve falhar (Red)

- [ ] Task 4: Implementar OverlaysSection (AC: 1, 3)
  - [ ] Criar `src/components/design-system/overlays-section.tsx` com `"use client"`
  - [ ] `Dialog`: trigger button + DialogContent com DialogTitle e descrição
  - [ ] `Sheet`: trigger button + SheetContent (side padrão)
  - [ ] `Tooltip`: TooltipProvider + Tooltip com trigger e TooltipContent
  - [ ] `Popover`: trigger button + PopoverContent com texto
  - [ ] `DropdownMenu`: trigger button + DropdownMenuContent com 3 DropdownMenuItems

- [ ] Task 5: Implementar FeedbackSection (AC: 2, 4)
  - [ ] Criar `src/components/design-system/feedback-section.tsx` com `"use client"`
  - [ ] `Toast`: botão "Disparar Toast" que chama `toast("Exemplo de toast")` do Sonner
  - [ ] `Alert`: variante default + variante destructive (com AlertTitle e AlertDescription)
  - [ ] `Skeleton`: 3 linhas de placeholder com larguras variadas
  - [ ] `Progress`: barra estática com `value={60}`

- [ ] Task 6: Integrar no design-system/page.tsx (AC: 1, 2)
  - [ ] Importar e renderizar `OverlaysSection` e `FeedbackSection` na seção "Overlays"

- [ ] Task 7: Verificação final
  - [ ] `npm run test` — todos passando (Green)
  - [ ] `npm run build` — sem erros TypeScript
  - [ ] `npm run lint` — sem erros
  - [ ] Testar abertura/fechamento de Dialog e Sheet manualmente no browser

## Dev Notes

### Dependência

Esta story **depende de Story 2.1** (Design System Page Scaffold). A seção "Overlays" na rota `/design-system` deve existir antes de implementar o showcase. Implementar após Story 2.1 estar Done.

### Toaster Provider (Sonner)

shadcn/ui atual usa Sonner como provider de toast. O `<Toaster />` deve estar no root layout:

```typescript
import { Toaster } from "@/components/ui/sonner"
// Em src/components/providers.tsx ou diretamente em src/app/layout.tsx
```

Para disparar: `import { toast } from "sonner"` → `toast("Mensagem")`. [source-tree.md — `src/components/providers.tsx`]

### Dialog (Radix UI / shadcn)

```typescript
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog"
// IMPORTANTE: DialogTitle é obrigatório para acessibilidade (Radix UI Aria requirement)
```

Overlays Radix usam portals — renderizam em `document.body`. Testing Library detecta portals nativamente. [tech-stack.md — Radix UI com WCAG AA]

### Tooltip requer TooltipProvider

```typescript
import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "@/components/ui/tooltip"

<TooltipProvider>
  <Tooltip>
    <TooltipTrigger>Hover aqui</TooltipTrigger>
    <TooltipContent>Conteúdo do tooltip</TooltipContent>
  </Tooltip>
</TooltipProvider>
```

### Alert variantes

```typescript
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
// variant="default|destructive"
```

### Gotcha: text-foreground em containers de tema (Story 2.2)

Em qualquer container com classe de tema explícita (`.light` / `.dark`), **todos** os elementos de texto precisam de `text-foreground` ou `text-muted-foreground` explícitos. Aplicável especialmente a `Alert` e labels dentro de overlays que possam ser renderizados com tema variável.

```typescript
// ✅ Correto
<span className="text-sm text-foreground">Texto legível em qualquer tema</span>

// ❌ Errado — invisível em dark mode
<span className="text-sm">Texto</span>
```

[Lição aprendida Story 2.2 — QA re-review `77d417c`]

### Client Component obrigatório

Todos os overlays e toast requerem `"use client"` (estado de abertura gerenciado por Radix UI internamente). [coding-standards.md#2-padrões-de-componentes-react]

### Testing — Portals Radix

Para testar Dialog/Sheet com Testing Library:

```typescript
import { render, screen } from "@testing-library/react"
import userEvent from "@testing-library/user-event"

it("trigger do Dialog está presente", async () => {
  render(<OverlaysSection />)
  const trigger = screen.getByRole("button", { name: /abrir dialog/i })
  expect(trigger).toBeInTheDocument()
})
```

[coding-standards.md#4-padrão-de-testes-tdd]

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-03-27 | 1.0 | Story criada | River (@sm) |
| 2026-03-27 | 1.1 | Validação PO — GO (9/10). Should-fix aplicado: dependência de Story 2.1 adicionada aos Dev Notes | Pax (@po) |
| 2026-03-27 | 1.2 | Sincronizado ao ClickUp — Task ID: 868j1uede (https://app.clickup.com/t/868j1uede) | Pax (@po) |
| 2026-03-27 | 1.3 | Re-validação PO — GO (9.5/10). Should-fix aplicado: gotcha text-foreground (lição Story 2.2) adicionado aos Dev Notes | Pax (@po) |

## Dev Agent Record

_A ser preenchido pelo @dev_

## QA Results

_A ser preenchido pelo @qa_
