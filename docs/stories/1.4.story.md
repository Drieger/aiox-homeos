# Story 1.4: Global State & Theme

## Status

InReview

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["docs/architecture/coding-standards.md", "docs/architecture/source-tree.md"]
```

## Story

**As a** usuário,
**I want** alternar entre tema claro e escuro com minha preferência salva,
**so that** o sistema respeite minha escolha visual entre sessões.

## Complexity

**Estimativa:** S (Small) — Providers simples, store Zustand com expansão do existente, ThemeToggle com shadcn/ui. Depende de 1.1 e 1.3.

## Scope

**IN:**
- `next-themes` + `ThemeProvider` no root layout
- Expandir `src/store/ui-store.ts` com campo `theme` (sem recriar)
- `src/components/layout/theme-toggle.tsx` com dropdown 3 opções
- `src/components/providers.tsx` compondo ThemeProvider + QueryClientProvider
- Atualizar `src/app/layout.tsx` com `<Providers>` e `suppressHydrationWarning`
- Canary page com nome "HomeOS", ThemeToggle e link para `/design-system`
- Testes unitários TDD para o store Zustand (mutations de tema e sidebar)

**OUT:**
- Design System completo (Epic 2)
- Preferências de usuário persistidas em banco de dados
- Internacionalização (i18n) ou múltiplos idiomas
- Sistema de notificações ou toasts globais
- Qualquer lógica de autenticação

## Risks

| Risco | Probabilidade | Impacto | Mitigação |
|-------|--------------|---------|-----------|
| Hydration mismatch com next-themes sem `suppressHydrationWarning` | Alta | Médio | Documentado em Dev Notes como obrigatório; Task 7 cobre explicitamente |
| Story 1.3 pode ter criado `ui-store.ts` com estrutura diferente | Média | Médio | Dev Notes instrui verificar e expandir, não recriar |
| `QueryClient` compartilhado entre SSR requests se criado fora do componente | Alta | Alto | Dev Notes documenta uso obrigatório de `useState(() => new QueryClient())` |
| Depende de Story 1.1 (shadcn/ui) e 1.3 (estrutura de providers) | Alta | Bloqueante | Executar somente após Stories 1.1 e 1.3 Done |

## Acceptance Criteria

1. `next-themes` instalado e `ThemeProvider` configurado no root layout
2. `src/store/ui-store.ts` define store Zustand com: `sidebarCollapsed` (boolean) e `theme` (light/dark/system)
3. `src/components/layout/theme-toggle.tsx` alterna tema e reflete estado atual
4. Tema persiste entre reloads via `next-themes` + localStorage
5. `QueryClientProvider` do React Query envolve o app no root layout
6. Todos os providers compostos em `src/components/providers.tsx` (client component único)
7. Canary page exibe: nome "HomeOS", tema toggle funcional e link para `/design-system`
8. Testes unitários (TDD) para o Zustand store: validar mutations de `sidebarCollapsed` e `theme`

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [x] Task 1: Escrever testes unitários do Zustand store (TDD — FIRST) (AC: 8)
  - [x] Criar `tests/unit/store/ui-store.test.ts` ANTES de criar o store
  - [x] Teste: `sidebarCollapsed` inicia como `false`
  - [x] Teste: `toggleSidebar()` alterna de false para true e vice-versa
  - [x] Teste: `setSidebarCollapsed(true)` define como true
  - [x] Teste: `theme` inicia como `"system"`
  - [x] Teste: `setTheme("dark")` define tema como dark
  - [x] Executar `npm run test` — deve falhar (Red)

- [x] Task 2: Instalar dependências (AC: 1, 5)
  - [x] `npm install next-themes`
  - [x] `npm install @tanstack/react-query`
  - [x] Confirmar que `zustand` já foi instalado na Story 1.3

- [x] Task 3: Criar/atualizar `src/store/ui-store.ts` completo (AC: 2)
  - [x] Se a Story 1.3 criou versão mínima: expandir para incluir `theme`
  - [x] Store deve ter: `sidebarCollapsed` (boolean, default: false) + `theme` ("light"|"dark"|"system", default: "system")
  - [x] Mutations: `toggleSidebar()`, `setSidebarCollapsed(boolean)`, `setTheme(theme)`
  - [x] Usar `persist` middleware do Zustand para localStorage
  - [x] Executar testes do Task 1 — deve passar (Green)

- [x] Task 4: Instalar componentes shadcn/ui necessários (AC: 3)
  - [x] `npx shadcn@latest add dropdown-menu` — para theme toggle com opções
  - [x] `npx shadcn@latest add button` — se ainda não instalado

- [x] Task 5: Criar `src/components/layout/theme-toggle.tsx` (AC: 3, 4)
  - [x] Client Component com `"use client"`
  - [x] Usa `useTheme()` do `next-themes` para ler e definir tema
  - [x] Exibe ícone Sun (claro), Moon (escuro) ou Monitor (system) — Lucide React
  - [x] Dropdown com 3 opções: "Claro", "Escuro", "Sistema"
  - [x] Estado visual reflete o tema atual

- [x] Task 6: Criar `src/components/providers.tsx` (AC: 6)
  - [x] Client Component único (`"use client"`) que compõe todos os providers
  - [x] Ordem de composição: `ThemeProvider` (next-themes) → `QueryClientProvider`
  - [x] `ThemeProvider` configurado: `attribute="class"`, `defaultTheme="system"`, `enableSystem`, `disableTransitionOnChange`
  - [x] `QueryClientProvider` com `new QueryClient()` criado fora do componente (ou com `useState`)

- [x] Task 7: Atualizar `src/app/layout.tsx` (AC: 1, 5, 6)
  - [x] Importar e usar `<Providers>` do `src/components/providers.tsx`
  - [x] Adicionar `suppressHydrationWarning` ao `<html>` tag (necessário para next-themes)
  - [x] Mantidos Geist fonts (decisão D1 — projeto já configurado com Geist na Story 1.1)
  - [x] Metadata básica: title "HomeOS", description

- [x] Task 8: Criar canary page (AC: 7)
  - [x] Criada canary específica em `src/app/page.tsx` (decisão D3 — substituiu redirect)
  - [x] A canary page exibe: nome "HomeOS", `ThemeToggle` funcional, links para `/books` e `/design-system`
  - [x] Verificar que toggle de tema funciona visualmente em `npm run dev`

- [x] Task 9: Verificação final
  - [x] `npm run dev` — testar toggle claro/escuro funcional
  - [x] Recarregar página — tema deve persistir
  - [x] `npm run test` — 14/14 testes passando (4 test files)
  - [x] `npm run build` — sem erros TypeScript, 4 rotas geradas
  - [x] `npm run lint` — sem erros

## Dev Notes

### Dependências e Versões

```bash
npm install next-themes @tanstack/react-query
# zustand já instalado na Story 1.3
```

[Source: docs/architecture/tech-stack.md#estado-dados]

### `src/store/ui-store.ts` — Store Completo

```typescript
import { create } from "zustand";
import { persist } from "zustand/middleware";

type Theme = "light" | "dark" | "system";

interface UIStore {
  sidebarCollapsed: boolean;
  theme: Theme;
  setSidebarCollapsed: (collapsed: boolean) => void;
  toggleSidebar: () => void;
  setTheme: (theme: Theme) => void;
}

export const useUIStore = create<UIStore>()(
  persist(
    (set) => ({
      sidebarCollapsed: false,
      theme: "system",
      setSidebarCollapsed: (collapsed) => set({ sidebarCollapsed: collapsed }),
      toggleSidebar: () =>
        set((state) => ({ sidebarCollapsed: !state.sidebarCollapsed })),
      setTheme: (theme) => set({ theme }),
    }),
    { name: "ui-store" }
  )
);
```

**Nota:** Se a Story 1.3 criou uma versão mínima deste store, esta story o expande — não recria do zero. Verificar se o arquivo já existe.

[Source: docs/architecture/source-tree.md#estado-global-client-side-zustand]

### `src/components/providers.tsx`

```typescript
"use client";

import { ThemeProvider } from "next-themes";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { useState } from "react";

export function Providers({ children }: { children: React.ReactNode }) {
  const [queryClient] = useState(() => new QueryClient());

  return (
    <ThemeProvider
      attribute="class"
      defaultTheme="system"
      enableSystem
      disableTransitionOnChange
    >
      <QueryClientProvider client={queryClient}>
        {children}
      </QueryClientProvider>
    </ThemeProvider>
  );
}
```

**Importante:** `QueryClient` deve ser criado com `useState(() => new QueryClient())` — não fora do componente — para evitar compartilhamento de state entre requisições em SSR.

[Source: docs/architecture/source-tree.md#componentes-providers]

### `src/app/layout.tsx` — Root Layout

```typescript
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import { Providers } from "@/components/providers";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "HomeOS",
  description: "Seu sistema pessoal de produtividade",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="pt-BR" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
```

**`suppressHydrationWarning` é obrigatório** no `<html>` quando usando next-themes — o provider modifica o atributo `class` no lado do cliente e o React emite warning sem isso.

[Source: docs/architecture/tech-stack.md#estilo-ui]

### `src/components/layout/theme-toggle.tsx`

```typescript
"use client";

import { useTheme } from "next-themes";
import { Moon, Sun, Monitor } from "lucide-react";
import { Button } from "@/components/ui/button";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button variant="ghost" size="icon">
          <Sun className="h-4 w-4 rotate-0 scale-100 transition-all dark:-rotate-90 dark:scale-0" />
          <Moon className="absolute h-4 w-4 rotate-90 scale-0 transition-all dark:rotate-0 dark:scale-100" />
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        <DropdownMenuItem onClick={() => setTheme("light")}>
          <Sun className="mr-2 h-4 w-4" /> Claro
        </DropdownMenuItem>
        <DropdownMenuItem onClick={() => setTheme("dark")}>
          <Moon className="mr-2 h-4 w-4" /> Escuro
        </DropdownMenuItem>
        <DropdownMenuItem onClick={() => setTheme("system")}>
          <Monitor className="mr-2 h-4 w-4" /> Sistema
        </DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

### Testing

- **Localização**: `tests/unit/store/ui-store.test.ts`
- **Framework**: Vitest (sem DOM — store é lógica pura)
- **Mock necessário**: `localStorage` (Zustand `persist` usa localStorage)
- **Padrão TDD**: testes escritos ANTES do store

```typescript
import { describe, it, expect, beforeEach } from "vitest";
import { useUIStore } from "@/store/ui-store";

// Resetar store antes de cada teste
beforeEach(() => {
  useUIStore.setState({
    sidebarCollapsed: false,
    theme: "system",
  });
});

describe("ui-store", () => {
  it("sidebarCollapsed inicia como false", () => {
    expect(useUIStore.getState().sidebarCollapsed).toBe(false);
  });

  it("toggleSidebar alterna o estado", () => {
    useUIStore.getState().toggleSidebar();
    expect(useUIStore.getState().sidebarCollapsed).toBe(true);
    useUIStore.getState().toggleSidebar();
    expect(useUIStore.getState().sidebarCollapsed).toBe(false);
  });

  it("setTheme define o tema corretamente", () => {
    useUIStore.getState().setTheme("dark");
    expect(useUIStore.getState().theme).toBe("dark");
  });
});
```

[Source: docs/architecture/coding-standards.md#testes-unitários-vitest]

### Dependência de Story Anterior

- Depende da Story 1.1: shadcn/ui, estrutura de diretórios
- Depende da Story 1.3: `AppSidebar` pode usar o store expandido nesta story (expansão do store mínimo criado na 1.3)
- **Importante:** Se Story 1.3 criou `ui-store.ts` mínimo, atualizar — não recriar

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-03-25 | 1.0 | Story criada | River (@sm) |
| 2026-03-27 | 1.1 | Validação PO — GO (8/10). Should-fix: estimativa de complexidade, riscos, escopo OUT | Pax (@po) |
| 2026-03-27 | 1.2 | Adicionadas seções Complexity (S), Scope (IN/OUT) e Risks — correção should-fix do @po | River (@sm) |
| 2026-03-27 | 1.3 | Implementação completa Pre-flight → YOLO mode — 14/14 testes passando, build limpo, lint clean | Dex (@dev) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6 (Dex @dev, Pre-flight → YOLO mode)

### Debug Log References

- Nenhum bloqueio crítico.
- D2: `asChild` incompatível com base-nova/@base-ui/react — ThemeToggle usa `buttonVariants` diretamente no `DropdownMenuTrigger` (mais simples e correto).
- D1: Geist fonts mantidas (Inter não era AC, projeto já configurado com Geist).
- D3: Canary page em `/` — substitui redirect; página fora do route group `(app)`.

### Completion Notes List

- `next-themes@0.4.6` + `@tanstack/react-query@5.95.2` instalados.
- `ui-store.ts` expandido com `theme: Theme` e `setTheme()` — `sidebarCollapsed` e sidebar mutations preservados.
- `dropdown-menu.tsx` instalado via shadcn (base-nova/@base-ui/react/menu).
- `Providers` compõe ThemeProvider → QueryClientProvider com `useState(() => new QueryClient())` para segurança SSR.
- Root layout: `suppressHydrationWarning` adicionado ao `<html>`, `lang="pt-BR"`, metadata "HomeOS".
- Canary page em `/` substitui redirect — exibe nome, ThemeToggle e links de navegação.
- Build limpo: 4 rotas estáticas (/, /_not-found, /books, /design-system).

### File List

**Criados:**
- `homeos/tests/unit/store/ui-store.test.ts`
- `homeos/src/components/layout/theme-toggle.tsx`
- `homeos/src/components/providers.tsx`
- `homeos/src/components/ui/dropdown-menu.tsx`

**Modificados:**
- `homeos/src/store/ui-store.ts` — expandido com `theme` e `setTheme`
- `homeos/src/app/layout.tsx` — Providers, suppressHydrationWarning, metadata, lang
- `homeos/src/app/page.tsx` — canary page (substituiu redirect)
- `homeos/package.json` — next-themes, @tanstack/react-query adicionados
- `homeos/package-lock.json`

## QA Results

### QA Gate — 2026-03-27 | Quinn (@qa)

**Verdict: ✅ PASS with CONCERNS**

**Acceptance Criteria:** 8/8 atendidos
**Tests:** 14/14 passando (4 test files)
**Lint:** Clean
**Security:** Nenhum concern identificado

**Checks:**
- [x] Code review — código limpo, JSDoc presente, decisões D1/D2/D3 documentadas
- [x] Unit tests — 14/14 passando, TDD Red→Green confirmado
- [x] Acceptance criteria — 8/8 ACs implementados corretamente
- [x] No regressions — testes da Story 1.3 continuam passando
- [x] Performance — `disableTransitionOnChange`, `useState(() => new QueryClient())` SSR-safe
- [x] Security — componentes UI puros, sem concerns
- [x] Documentation — JSDoc em todos os arquivos, Dev Agent Record completo

**Concern MEDIUM (tech debt — não bloqueante):**
- `useUIStore.theme` nunca é atualizado pelo `ThemeToggle` (que usa `useTheme().setTheme` do next-themes)
- Campo `theme` no Zustand está desconectado do estado real do next-themes
- Risco atual baixo (nenhum consumidor do campo ainda); risco futuro médio se lógica depender desse valor
- Recomendação: sincronizar via `useEffect` + listener do next-themes quando houver consumidor real

**Aprovado para `@devops *push`.**
