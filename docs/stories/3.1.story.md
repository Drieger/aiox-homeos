# Story 3.1: KB Foundation — Schema & Migrations

## Status

InReview

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["docs/architecture/tech-stack.md", "docs/architecture/source-tree.md"]
```

## Story

**As a** usuário do HomeOS,
**I want** que o módulo Knowledge Base tenha sua estrutura de dados inicializada,
**so that** eu possa começar a criar documentos com persistência local.

## Complexity

**Estimativa:** M (Medium) — schema, migration, FTS5 com triggers, nova rota e sidebar item.

## Scope

**IN:**
- Instalar as 5 novas dependências do Epic 3 (`@uiw/react-md-editor`, `react-markdown`, `remark-gfm`, `rehype-highlight`, `@tailwindcss/typography`)
- Criar `src/lib/db/schema/kb.ts` com tabelas: `notebooks`, `documents`, `tags`, `document_tags`, `document_links`
- Atualizar `src/lib/db/schema.ts` para re-exportar o schema KB (barrel pattern)
- Gerar e aplicar migration via `drizzle-kit` (tabelas regulares)
- Criar FTS5 virtual table + 3 triggers (INSERT/UPDATE/DELETE) via SQL raw no singleton do DB
- Criar índice `idx_documents_title` em `documents.title` via SQL raw
- Criar rota `/kb` com página placeholder que retorna 200 (sem erros 404)
- Adicionar item "Knowledge Base" na sidebar (`app-sidebar.tsx`) com ícone `BookOpen` e href `/kb`
- Registrar `@tailwindcss/typography` no `globals.css` via `@plugin "@tailwindcss/typography"` (sintaxe Tailwind v4)
- Testes de integração para o schema KB

**OUT:**
- Implementação de qualquer feature do módulo KB (apenas fundação)
- Componentes KB (kb-sidebar, document-list, editor — stories 3.2–3.7)
- UI real da página `/kb` (apenas scaffold)
- Seed data

## Dependencies

- Story 2.5 (Done) — sidebar e layout estabilizados
- Epic 3 PRD: `docs/prd/epic-3-knowledge-base.md`
- Notas arquiteturais validadas por @architect (v1.1 do epic)

## Risks

| Risco | Probabilidade | Impacto | Mitigação |
|-------|--------------|---------|-----------|
| FTS5 triggers incompatíveis com better-sqlite3 | Baixa | Alto | Usar `db.$client.exec()` para SQL raw síncrono |
| Conflito de schema entre barrel e Drizzle types | Baixa | Médio | Re-exportar com `export * from "./schema/kb"` — Drizzle infere corretamente |
| `@tailwindcss/typography` sem suporte a Tailwind v4 | Baixa | Médio | Verificar versão do pacote; v4 usa `@plugin` em CSS, não `tailwind.config.js` |

## Acceptance Criteria

1. Schema Drizzle criado em `src/lib/db/schema/kb.ts` com tabelas: `notebooks`, `documents`, `tags`, `document_tags`, `document_links`; todos os campos conforme o schema do epic
2. Todos os IDs usam ULID gerado via `ulidx` (padrão do projeto)
3. Migration aplicada com sucesso via `npm run db:generate` + `npm run db:migrate` — banco atualizado sem erros
4. Virtual table FTS5 `documents_fts` criada com 3 triggers (INSERT/UPDATE/DELETE em `documents`) via SQL raw no singleton do DB; índice `idx_documents_title` criado
5. Rota `/kb` existe e retorna página placeholder sem erros 404 (pode ser texto simples ou `<h1>Knowledge Base</h1>`)
6. Item "Knowledge Base" adicionado à navegação da sidebar (`app-sidebar.tsx`) com ícone `BookOpen` e href `/kb`, visível tanto no modo expandido (com label) quanto colapsado (apenas ícone com tooltip)

## Design System Impact

```
uses_existing_components_only: true
new_components: []
new_tokens: []
ux_approval_required: false
ux_approval_evidence: "N/A — story é infraestrutura/schema sem novos componentes visuais"
```

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI não está habilitado em `core-config.yaml`.
> Quality validation usará processo de revisão manual.

## Tasks / Subtasks

- [x] Task 1: Instalar dependências do Epic 3 (AC: implícito)
  - [x] `npm install @uiw/react-md-editor react-markdown remark-gfm rehype-highlight @tailwindcss/typography`
  - [x] Verificar que não há conflitos de versão com React 19 e Next.js 16

- [x] Task 2: Criar schema KB (AC: 1, 2)
  - [x] Criar diretório `src/lib/db/schema/`
  - [x] Criar `src/lib/db/schema/kb.ts` com tabelas: `notebooks`, `documents`, `tags`, `document_tags`, `document_links`
  - [x] IDs: `ulid()` via `ulidx` em todos os `id` primários
  - [x] Timestamps: `integer("created_at").$defaultFn(() => Date.now())` (Unix ms)
  - [x] Atualizar `src/lib/db/schema.ts`: substituir `export {}` por `export * from "./schema/kb"`

- [x] Task 3: Gerar e aplicar migration (AC: 3)
  - [x] Executar `npm run db:generate`
  - [x] Verificar o SQL gerado (5 tabelas presentes)
  - [x] Executar `npm run db:migrate`
  - [x] Confirmar que banco foi criado sem erros

- [x] Task 4: FTS5 + triggers + índice via SQL raw (AC: 4)
  - [x] Em `src/lib/db/index.ts`, após `drizzle()`, usar `sqlite.exec()` para criar:
    - Virtual table: `CREATE VIRTUAL TABLE IF NOT EXISTS documents_fts USING fts5(...)`
    - Trigger INSERT: `documents_ai`
    - Trigger DELETE: `documents_ad`
    - Trigger UPDATE: `documents_au`
    - Índice: `CREATE INDEX IF NOT EXISTS idx_documents_title ON documents(title)`
  - [x] Usar `DROP TRIGGER IF EXISTS` antes de cada `CREATE TRIGGER` (triggers não suportam `IF NOT EXISTS`)

- [x] Task 5: Registrar typography plugin (AC: implícito para stories futuras)
  - [x] Em `src/app/globals.css`, adicionar `@plugin "@tailwindcss/typography"` após `@import "tailwindcss"`

- [x] Task 6: Página placeholder `/kb` (AC: 5)
  - [x] Criar `src/app/(app)/kb/page.tsx` com Server Component mínimo

- [x] Task 7: Item KB na sidebar (AC: 6)
  - [x] Em `src/components/layout/app-sidebar.tsx`, adicionar `{ label: "Knowledge Base", href: "/kb", icon: BookOpen }` ao array `navItems`
  - [x] Importar `BookOpen` de `lucide-react`

- [x] Task 8: Testes (AC: 1, 2, 4, 5, 6)
  - [x] Criar `tests/integration/kb/schema.test.ts` — valida que as 5 tabelas existem no DB após migration
  - [x] Criar `tests/integration/kb/fts.test.ts` — INSERT em `documents` → consulta FTS5 retorna resultado
  - [x] Criar `tests/component/kb/kb-sidebar-nav.test.tsx` — item "Knowledge Base" presente na sidebar

- [x] Task 9: Verificação final
  - [x] `npm run test` — 57 testes passando (47 existentes + 10 novos)
  - [x] `npm run build` — compilado com sucesso, TypeScript OK, rota /kb gerada
  - [x] `npm run lint` — sem erros

## Dev Notes

### Schema path — adaptação do epic

O epic define `src/db/schema/kb.ts` mas a estrutura real do projeto é `src/lib/db/`. Adaptar para `src/lib/db/schema/kb.ts`. O `src/lib/db/index.ts` já importa `* as schema from "./schema"` — ao transformar `schema.ts` em barrel file, o Drizzle receberá todos os exports nomeados corretamente.

### Drizzle Schema — referência

```typescript
// src/lib/db/schema/kb.ts
import { sqliteTable, text, integer, primaryKey } from "drizzle-orm/sqlite-core";
import { ulid } from "ulidx";

export const notebooks = sqliteTable("notebooks", {
  id: text("id").primaryKey().$defaultFn(() => ulid()),
  name: text("name").notNull(),
  createdAt: integer("created_at").notNull().$defaultFn(() => Date.now()),
  updatedAt: integer("updated_at").notNull().$defaultFn(() => Date.now()),
});

export const documents = sqliteTable("documents", {
  id: text("id").primaryKey().$defaultFn(() => ulid()),
  notebookId: text("notebook_id").notNull().references(() => notebooks.id),
  title: text("title").notNull().default("Sem título"),
  content: text("content").notNull().default(""),
  createdAt: integer("created_at").notNull().$defaultFn(() => Date.now()),
  updatedAt: integer("updated_at").notNull().$defaultFn(() => Date.now()),
});

export const tags = sqliteTable("tags", {
  id: text("id").primaryKey().$defaultFn(() => ulid()),
  name: text("name").notNull().unique(),
});

export const documentTags = sqliteTable("document_tags", {
  documentId: text("document_id").notNull().references(() => documents.id, { onDelete: "cascade" }),
  tagId: text("tag_id").notNull().references(() => tags.id, { onDelete: "cascade" }),
}, (t) => [primaryKey({ columns: [t.documentId, t.tagId] })]);

export const documentLinks = sqliteTable("document_links", {
  sourceId: text("source_id").notNull().references(() => documents.id, { onDelete: "cascade" }),
  targetId: text("target_id").notNull().references(() => documents.id, { onDelete: "cascade" }),
}, (t) => [primaryKey({ columns: [t.sourceId, t.targetId] })]);
```

### FTS5 + Triggers — padrão no singleton

```typescript
// Em src/lib/db/index.ts, após drizzle(sqlite, { schema }):
sqlite.exec(`
  CREATE VIRTUAL TABLE IF NOT EXISTS documents_fts USING fts5(
    title,
    content,
    content=documents,
    content_rowid=rowid
  );

  DROP TRIGGER IF EXISTS documents_ai;
  CREATE TRIGGER documents_ai AFTER INSERT ON documents BEGIN
    INSERT INTO documents_fts(rowid, title, content)
    VALUES (new.rowid, new.title, new.content);
  END;

  DROP TRIGGER IF EXISTS documents_ad;
  CREATE TRIGGER documents_ad AFTER DELETE ON documents BEGIN
    INSERT INTO documents_fts(documents_fts, rowid, title, content)
    VALUES ('delete', old.rowid, old.title, old.content);
  END;

  DROP TRIGGER IF EXISTS documents_au;
  CREATE TRIGGER documents_au AFTER UPDATE ON documents BEGIN
    INSERT INTO documents_fts(documents_fts, rowid, title, content)
    VALUES ('delete', old.rowid, old.title, old.content);
    INSERT INTO documents_fts(rowid, title, content)
    VALUES (new.rowid, new.title, new.content);
  END;

  CREATE INDEX IF NOT EXISTS idx_documents_title ON documents(title);
`);
```

### Página placeholder `/kb`

```typescript
// src/app/(app)/kb/page.tsx
export default function KbPage() {
  return (
    <div className="flex h-full items-center justify-center">
      <p className="text-muted-foreground">Knowledge Base — em construção</p>
    </div>
  );
}
```

### Sidebar — padrão existente

O `navItems` em `app-sidebar.tsx` é um array `as const`. Adicionar antes do item "Design System" para manter ordem lógica (ou após, conforme preferência visual):

```typescript
{
  label: "Knowledge Base",
  href: "/kb",
  icon: BookOpen,
},
```

`BookOpen` já está disponível em `lucide-react` (instalado).

### @tailwindcss/typography — Tailwind v4

```css
/* globals.css — adicionar após @import "tailwindcss" */
@plugin "@tailwindcss/typography";
```

**Não usar** `plugins: [require("@tailwindcss/typography")]` em `tailwind.config.js` — Tailwind v4 não usa esse arquivo.

### Testes de integração — padrão DB em memória

```typescript
// tests/integration/kb/schema.test.ts
import Database from "better-sqlite3";
import { drizzle } from "drizzle-orm/better-sqlite3";
import { migrate } from "drizzle-orm/better-sqlite3/migrator";
import * as schema from "@/lib/db/schema";

// Usar banco em memória para testes
const sqlite = new Database(":memory:");
const db = drizzle(sqlite, { schema });
migrate(db, { migrationsFolder: "./src/lib/db/migrations" });

// Testar que tabelas existem inserindo e lendo um notebook
it("cria notebook corretamente", async () => {
  // ...
});
```

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Completion Notes

- Schema KB criado em `src/lib/db/schema/kb.ts` com 5 tabelas (notebooks, documents, tags, document_tags, document_links); IDs ULID via `ulidx`
- `src/lib/db/schema.ts` convertido para barrel file: `export * from "./schema/kb"`
- Migration gerada (`0000_redundant_electro.sql`) e aplicada com sucesso
- FTS5 + 3 triggers + índice `idx_documents_title` criados via `sqlite.exec()` dentro de `initKbFts()` chamado no `createDb()` do singleton
- Typography plugin registrado em `globals.css` via `@plugin "@tailwindcss/typography"` (sintaxe Tailwind v4)
- Rota `/kb` criada como Server Component placeholder — rota aparece no build output
- Item "Knowledge Base" (ícone `BookOpen`) adicionado ao `navItems` da sidebar entre Home e Design System
- 10 novos testes: 3 de schema (insert notebook, insert document, unique constraint tags) + 4 de FTS5 (trigger insert, match conteúdo, trigger update, índice) + 3 de componente (render item, link href, ícone SVG)
- 4 dependências instaladas: `@uiw/react-md-editor`, `react-markdown`, `remark-gfm`, `rehype-highlight`, `@tailwindcss/typography`

### File List

- `homeos/src/lib/db/schema/kb.ts` — CRIADO
- `homeos/src/lib/db/schema.ts` — MODIFICADO (barrel re-export)
- `homeos/src/lib/db/index.ts` — MODIFICADO (initKbFts + chamada em createDb)
- `homeos/src/lib/db/migrations/0000_redundant_electro.sql` — CRIADO (drizzle-kit generate)
- `homeos/src/app/(app)/kb/page.tsx` — CRIADO
- `homeos/src/app/globals.css` — MODIFICADO (@plugin typography)
- `homeos/src/components/layout/app-sidebar.tsx` — MODIFICADO (BookOpen import + navItem KB)
- `homeos/tests/integration/kb/schema.test.ts` — CRIADO
- `homeos/tests/integration/kb/fts.test.ts` — CRIADO
- `homeos/tests/component/kb/kb-sidebar-nav.test.tsx` — CRIADO
- `homeos/package.json` — MODIFICADO (5 novas dependências)
- `homeos/package-lock.json` — MODIFICADO

## QA Results

### Review Date: 2026-03-28

### Reviewed By: Quinn (@qa)

### Code Quality Assessment

Implementação limpa e sólida. Padrão de barrel para schemas é bem estruturado e preparado para escalabilidade multi-módulo. `initKbFts()` é idempotente por design (`IF NOT EXISTS` para FTS, `DROP IF EXISTS` para triggers) — seguro em hot-reload. Testes de integração com banco em memória e `// @vitest-environment node` seguem o padrão correto para SQLite. Código de produção totalmente livre de lógica de negócio (apenas fundação/infraestrutura conforme escopo).

### Refactoring Performed

Nenhum.

### Compliance Check

- Coding Standards: ✓ Sem `"use client"` desnecessário, imports absolutos, TypeScript strict
- Project Structure: ✓ Schema em `src/lib/db/schema/`, testes em `tests/integration/kb/` e `tests/component/kb/`
- Testing Strategy: ✓ Integration tests com banco em memória; environment node correto; lint limpo

### AC Traceability

| AC | Status | Evidência |
|----|--------|-----------|
| AC1 — Schema 5 tabelas em `src/lib/db/schema/kb.ts` | ✅ PASS | `schema/kb.ts` — notebooks, documents, tags, document_tags, document_links; campos corretos |
| AC2 — IDs via ULID (`ulidx`) | ✅ PASS | `$defaultFn(() => ulid())` em todos os campos `id` primários |
| AC3 — Migration via drizzle-kit sem erros | ✅ PASS | `0000_redundant_electro.sql` gerado e aplicado com sucesso; 5 tabelas confirmadas |
| AC4 — FTS5 + 3 triggers + índice `title` via SQL raw | ✅ PASS | `initKbFts()` em `index.ts` — virtual table, triggers AI/AD/AU, `idx_documents_title` |
| AC5 — Rota `/kb` retorna 200 sem 404 | ✅ PASS | `src/app/(app)/kb/page.tsx` — aparece no build output como rota estática |
| AC6 — Item KB na sidebar (expandido + colapsado) | ✅ PASS | `BookOpen` + `{ label: "Knowledge Base", href: "/kb" }` no `navItems`; `SidebarNavItem` existente já gerencia colapsado |

### Security Review

Sem concerns. Layer de schema/infraestrutura pura. Nenhum input de usuário, nenhum vetor XSS, nenhuma credencial hardcoded. Queries FTS nos testes usam `?` parametrizado.

### Performance Considerations

`initKbFts()` executa sincrono via `sqlite.exec()` na inicialização — custo único de ~1ms. Idempotente: hot-reloads não recriam a virtual table. Índice `idx_documents_title` presente para lookup de wiki-links.

### Issues Documented

**CONCERNS-1 (LOW):** `schema.test.ts:33` — `expect(rows).toHaveLength(1)` depende de ordem de execução dos testes. Se Vitest mudar ordem ou rodar em paralelo, o teste falha. Recomendado: usar banco isolado por teste com `beforeEach` ou filtrar por ID.

**CONCERNS-2 (LOW):** Trigger DELETE não está coberto por testes. O trigger `documents_ad` é implementado mas não verificado. Recomendado: adicionar teste `trigger DELETE: documento removido não aparece na busca FTS`.

**CONCERNS-3 (LOW):** `initKbFts()` falhará silenciosamente (ou com erro) se o app iniciar antes de `npm run db:migrate` ser executado. Aceitável para app local single-dev, mas pode causar confusão durante onboarding. Recomendado: documentar no README a ordem de setup.

### Improvements Checklist

- [ ] Isolar estado do banco por teste em `schema.test.ts` (usar `beforeEach` com novo DB em memória)
- [ ] Adicionar teste para `trigger DELETE` em `fts.test.ts`
- [ ] Documentar no README a ordem: `db:migrate` antes do primeiro `dev`

### Gate Status

Gate: **PASS**
Quality Score: 93/100

### Recommended Status

✅ Ready for Done — 3 concerns LOW documentados, nenhum bloqueia merge. Fundação KB sólida e escalável.

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-03-28 | 1.0 | Story criada a partir do Epic 3 (v1.1) | River (@sm) |
| 2026-03-28 | 1.1 | Status: Draft → Ready (GO, 9.5/10) | Pax (@po) |
| 2026-03-28 | 1.2 | Implementação completa — Status: InReview | Dex (@dev) |
