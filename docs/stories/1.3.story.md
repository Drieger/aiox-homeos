# Story 1.3: App Layout Shell

## Status

Ready

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["docs/architecture/source-tree.md", "docs/architecture/coding-standards.md"]
```

## Story

**As a** usuário,
**I want** navegar entre seções do HomeOS via sidebar persistente,
**so that** todas as features sejam acessíveis a partir de um ponto central de navegação.

## Complexity

**Estimativa:** M (Medium) — Componentes React com state management, rotas Next.js, comportamento responsivo mobile, testes de componente.

## Scope

**IN:**
- `AppSidebar` colapsável com navegação Biblioteca/Livros e Design System
- Route group `(app)` com layout compartilhado
- Redirect `/` → `/books`
- Comportamento mobile com Sheet (hamburger)
- Zustand store mínimo (`sidebarCollapsed`) — versão provisória para esta story
- Testes TDD de componente para AppSidebar

**OUT:**
- Conteúdo real das páginas `/books` e `/design-system` (placeholders apenas)
- Tema claro/escuro (Story 1.4)
- `QueryClientProvider` e `ThemeProvider` (Story 1.4)
- Autenticação ou rotas protegidas
- Página de configurações ou perfil de usuário

## Risks

| Risco | Probabilidade | Impacto | Mitigação |
|-------|--------------|---------|-----------|
| Zustand store criado aqui conflita com expansão da Story 1.4 | Média | Médio | Dev Notes documenta a estratégia: criar versão mínima, Story 1.4 expande |
| `usePathname()` requer Client Component — SSR limitado | Alta | Baixo | Esperado e documentado; AppSidebar é `"use client"` por design |
| Testes de componente com mocks do Next.js Router podem ser frágeis | Média | Médio | Dev Notes fornece template de mock do `next/navigation` |
| Depende de Story 1.1 (shadcn/ui, estrutura de diretórios) | Alta | Bloqueante | Executar somente após Story 1.1 Done |

## Acceptance Criteria

1. `src/components/layout/app-sidebar.tsx` renderiza sidebar com itens: "Biblioteca" (submenu: "Livros") e "Design System"
2. Sidebar colapsa para modo ícone via botão toggle — estado persistido no Zustand store
3. Rota ativa é destacada visualmente na sidebar
4. Layout root (`src/app/layout.tsx`) compõe sidebar + área de conteúdo principal
5. Em viewport < 768px: sidebar oculta com menu hamburger que abre Sheet (shadcn/ui)
6. Navegação usa `<Link>` do Next.js — sem full page reload
7. `/` redireciona para `/books`
8. Testes (TDD): pelo menos 1 teste de componente para `AppSidebar` validando renderização dos itens e estado de colapso

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [ ] Task 1: Escrever testes do componente AppSidebar (TDD — FIRST) (AC: 8)
  - [ ] Criar `tests/component/app-sidebar.test.tsx` ANTES de criar o componente
  - [ ] Teste 1: sidebar renderiza item "Biblioteca" e submenu "Livros"
  - [ ] Teste 2: sidebar renderiza item "Design System"
  - [ ] Teste 3: botão de toggle alterna estado de colapso
  - [ ] Executar `npm run test` — deve falhar (Red)

- [ ] Task 2: Instalar componentes shadcn/ui necessários (AC: 1, 5)
  - [ ] `npx shadcn@latest add sheet` — para sidebar mobile
  - [ ] `npx shadcn@latest add tooltip` — para modo colapsado (ícones com tooltip)
  - [ ] `npx shadcn@latest add separator` — para divisores na sidebar
  - [ ] `npx shadcn@latest add scroll-area` — para sidebar scrollável

- [ ] Task 3: Criar route group `(app)` com layout compartilhado (AC: 4)
  - [ ] Criar `src/app/(app)/layout.tsx` — layout com sidebar + main content
  - [ ] Criar `src/app/(app)/books/page.tsx` — placeholder (implementado no Epic 3)
  - [ ] Criar `src/app/(app)/design-system/page.tsx` — placeholder (implementado no Epic 2)

- [ ] Task 4: Criar `src/components/layout/app-sidebar.tsx` (AC: 1, 2, 3, 6)
  - [ ] Renderiza lista de itens de navegação com ícones Lucide
  - [ ] Item "Biblioteca" com submenu colapsável contendo "Livros" (link para `/books`)
  - [ ] Item "Design System" (link para `/design-system`)
  - [ ] Usa `usePathname()` do Next.js para destacar rota ativa
  - [ ] Lê `sidebarCollapsed` do Zustand store (Story 1.4 — usar valor default `false` por ora)
  - [ ] Em modo colapsado: exibe apenas ícones; em modo expandido: ícones + labels
  - [ ] Botão toggle muda `sidebarCollapsed` no store

- [ ] Task 5: Implementar comportamento mobile — Sheet (AC: 5)
  - [ ] Em viewport < 768px: sidebar principal oculta
  - [ ] Botão hamburger (ícone Menu do Lucide) visível apenas em mobile
  - [ ] Clicar no hamburger abre `Sheet` do shadcn/ui com o mesmo conteúdo da sidebar
  - [ ] Usar `useMediaQuery` ou CSS `hidden`/`md:block` para responsividade

- [ ] Task 6: Criar redirect `/` → `/books` (AC: 7)
  - [ ] Atualizar `src/app/page.tsx` para usar `redirect("/books")` do Next.js
  - [ ] Verificar que `/` não exibe conteúdo — redireciona imediatamente

- [ ] Task 7: Executar testes — Green (AC: 8)
  - [ ] Confirmar que os testes do Task 1 passam com a implementação completa
  - [ ] Executar `npm run test` — todos os testes devem passar
  - [ ] Executar `npm run lint` — sem erros

- [ ] Task 8: Verificação visual no browser
  - [ ] `npm run dev` e navegar para `http://localhost:3000`
  - [ ] Confirmar redirect para `/books`
  - [ ] Confirmar sidebar visível e navegação funcional
  - [ ] Testar collapse da sidebar
  - [ ] Testar viewport < 768px (DevTools mobile)

## Dev Notes

### Estrutura de Arquivos a Criar

```
src/
├── app/
│   ├── page.tsx                    # redirect("/books")
│   └── (app)/
│       ├── layout.tsx              # Sidebar + main content wrapper
│       ├── books/
│       │   └── page.tsx            # Placeholder para Epic 3
│       └── design-system/
│           └── page.tsx            # Placeholder para Epic 2
├── components/
│   └── layout/
│       ├── app-sidebar.tsx         # Sidebar colapsável principal
│       └── sidebar-nav-item.tsx    # Item de navegação com estado ativo
```

[Source: docs/architecture/source-tree.md#route-group-app]

### Route Group `(app)`

O diretório `(app)` é um **route group** do Next.js — o parêntese NÃO aparece na URL. Ele permite que `/books` e `/design-system` compartilhem o mesmo layout (com sidebar) sem afetar o root layout.

```
URL /books → src/app/(app)/books/page.tsx
URL /design-system → src/app/(app)/design-system/page.tsx
```

[Source: docs/architecture/source-tree.md#route-group-app]

### Zustand Store — Dependência com Story 1.4

O `AppSidebar` precisa do store `sidebarCollapsed`. Como a Story 1.4 cria o store, esta story deve:
1. Criar um store mínimo provisório OU
2. Implementar diretamente o `ui-store.ts` necessário para o sidebar funcionar

**Recomendação:** Criar `src/store/ui-store.ts` com apenas `sidebarCollapsed` nesta story. A Story 1.4 complementará com `theme` e os providers. Isso evita dependência bloqueante entre stories.

```typescript
// src/store/ui-store.ts — versão mínima para esta story
import { create } from "zustand";
import { persist } from "zustand/middleware";

interface UIStore {
  sidebarCollapsed: boolean;
  setSidebarCollapsed: (collapsed: boolean) => void;
  toggleSidebar: () => void;
}

export const useUIStore = create<UIStore>()(
  persist(
    (set) => ({
      sidebarCollapsed: false,
      setSidebarCollapsed: (collapsed) => set({ sidebarCollapsed: collapsed }),
      toggleSidebar: () =>
        set((state) => ({ sidebarCollapsed: !state.sidebarCollapsed })),
    }),
    { name: "ui-store" }
  )
);
```

[Source: docs/architecture/source-tree.md#estado-global-client-side-zustand]

### Instalar Zustand nesta story

```bash
npm install zustand
```

### AppSidebar — Estrutura de Navegação

```typescript
const navItems = [
  {
    label: "Biblioteca",
    icon: BookOpen,  // Lucide
    children: [
      { label: "Livros", href: "/books", icon: List }
    ]
  },
  {
    label: "Design System",
    href: "/design-system",
    icon: Palette
  }
];
```

### Rota Ativa — `usePathname`

```typescript
"use client";
import { usePathname } from "next/navigation";

// Dentro do componente:
const pathname = usePathname();
const isActive = (href: string) => pathname === href || pathname.startsWith(href + "/");
```

### Layout `(app)` — Estrutura Base

```tsx
// src/app/(app)/layout.tsx
import { AppSidebar } from "@/components/layout/app-sidebar";

export default function AppLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex h-screen">
      <AppSidebar />
      <main className="flex-1 overflow-auto p-6">
        {children}
      </main>
    </div>
  );
}
```

### Testing

- **Localização**: `tests/component/app-sidebar.test.tsx`
- **Framework**: Vitest + Testing Library (environment `jsdom`)
- **Padrão TDD**: testes escritos ANTES do componente
- **Mock necessário**: `next/navigation` (`usePathname`) deve ser mockado

```typescript
// Mock do usePathname no topo do arquivo de teste
vi.mock("next/navigation", () => ({
  usePathname: () => "/books",
  Link: ({ children, href }: { children: React.ReactNode; href: string }) =>
    <a href={href}>{children}</a>,
}));
```

- **O que testar**:
  - Renderização dos itens de navegação ("Biblioteca", "Livros", "Design System")
  - Estado de colapso: toggling via botão
  - Rota ativa visualmente destacada (classe CSS ou atributo `aria-current`)

[Source: docs/architecture/coding-standards.md#testes-de-componente-testing-library]

### Dependência de Story Anterior

Depende da Story 1.1 (project scaffold) estar completa — shadcn/ui instalado, estrutura de diretórios criada.

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-03-25 | 1.0 | Story criada | River (@sm) |
| 2026-03-27 | 1.1 | Validação PO — GO (8/10). Should-fix: estimativa de complexidade, riscos, escopo OUT | Pax (@po) |
| 2026-03-27 | 1.2 | Adicionadas seções Complexity (M), Scope (IN/OUT) e Risks — correção should-fix do @po | River (@sm) |

## Dev Agent Record

### Agent Model Used

_A ser preenchido pelo @dev_

### Debug Log References

_A ser preenchido pelo @dev_

### Completion Notes List

_A ser preenchido pelo @dev_

### File List

_A ser preenchido pelo @dev_

## QA Results

_A ser preenchido pelo @qa_
